---
# TODO(collection): enable when modules/plugins are added.
- name: Playbook to test hello_motd role.
  hosts: localhost
  remote_user: root
#  roles:
#    - hello_motd
  tasks:
    - name: 'NOOP'
      ansible.builtin.assert:
        that:
          - 1 == 1
